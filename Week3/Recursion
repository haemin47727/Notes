Recursion
-method that calls the same method in its method body

ex) int x = factorial(10);
 }
 static int factorial(int x){
    int total;
    if(x == 1)
        return 1;
    total = factorial(x - 1) * x;
    return total;